# redirect_forward 패키지 — 1-5(검증되지 않은 리다이렉트와 포워드) 전용 스캔 엔진
#
# 범위: Reflected 케이스만.
#   요청에 실어보낸 목적지 파라미터 값이 "같은 요청/응답 왕복" 안에서 검증 없이
#   그대로 Location 헤더(서버 사이드 리다이렉트) 또는 응답 본문의 meta refresh /
#   JS location 대입(클라이언트 사이드 리다이렉트)에 반영되는지를 확인한다.
#
#   Stored 케이스(예: 마이페이지 설정, 이메일 인증 링크 등으로 서버에 저장된 리다이렉트
#   목적지가 이후 별도 요청에서 실행되는 경우)는 "생성 → 저장 확인 → 트리거" 3단계
#   흐름이 따로 필요해 이 모듈의 범위 밖이다 (필요 시 verifier.py류 후속 검증 모듈로 확장).
